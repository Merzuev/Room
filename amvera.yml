meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12.3 # Укажи ту версию Python, что реально используется

build:
  requirementsPath: requirements.txt

run:
  command: gunicorn Room.wsgi:application --bind 0.0.0.0:80
  persistenceMount: /data
  containerPort: 80
